/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Kotlin application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.4.2/userguide/building_java_projects.html
 */

plugins {
    // Apply the org.jetbrains.kotlin.jvm Plugin to add support for Kotlin.
    id 'org.jetbrains.kotlin.jvm' version '1.5.31'

    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
}

ext {
    versionJVM = '1.8'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Align versions of all Kotlin components
    implementation platform('org.jetbrains.kotlin:kotlin-bom')

    // Use the Kotlin JDK 8 standard library.
    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk8'

    // This dependency is used by the application.
    implementation 'com.google.guava:guava:30.1.1-jre'

    // Use the Kotlin test library.
    testImplementation 'org.jetbrains.kotlin:kotlin-test'

    // Use the Kotlin JUnit integration.
    testImplementation 'org.jetbrains.kotlin:kotlin-test-junit'
}

// compileKotlin {
//     kotlinOptions.jvmTarget = $ { versionJVM }
// }
// compileTestKotlin {
//     kotlinOptions.jvmTarget = $ { versionJVM }
// }

application {
    // Define the main class for the application.
    mainClass = 'ezProject.AppKt'
// 'ezProject.TaskKt',
// 'ezProject.AppKt'
}

// sourceSets {
//     main {
//         kotlin {
//             srcDirs = ['src/main/kotlin/ezProject']
//         }
//     }

//     test {
//         kotlin {
//             srcDirs = ['test/kotlin/ezProject']
//         }
//     }
// }

// sourceSets {
//     main.kotlin.srcDirs = ['src']
//     test.kotlin.srcDirs = ['test']
//     main.resources.srcDirs = ['resources']
// }

configurations {
    detekt
}

// gradle command to be run below - in a terminal
// gradle detekt --warning-mode all

// def detektTask = tasks.register('detekt', JavaExec) {
//     mainClass = 'io.gitlab.arturbosch.detekt.cli.Main'
//     classpath = configurations.detekt

    // def input = "$projectDir"
    // def config = "$projectDir/detekt.yml"
    // def exclude = '.*/build/.*,.*/resources/.*'
    // def params = [ '-i', input, '-c', config, '-ex', exclude]

// args(params)
// }

dependencies {
    detekt 'io.gitlab.arturbosch.detekt:detekt-cli:1.21.0-RC1'
}

// Remove this line if you don't want to run detekt on every build
// check.dependsOn detektTask

jar {
    manifest {
        attributes 'Main-Class': 'ezProject.AppKt'
    }
}

// TODO: seek to generalise the version numbers, so that they can be updated at the top
